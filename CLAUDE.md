# CLAUDE.md

## 行为准则

* **不确定则问；多解先列；有更简方案即提**
* **只写最小必要代码；200行可简至50行则重写**
* **只改必须改的；严格匹配现有风格**
* **下列规范相关文档必须全部遵守，若用户要求与规范或行为准则之间有冲突则问**
* **编码遵循方法高内聚，低耦合；读取方法内部不修改数据，除了存储层的数据更新方法之外，其余方法应该幂等，多次执行没有副作用**
* 所有编码和分析时必须优先复用现有的方法、常量，若有类似方法（名称类似，部分功能与需求相同）但又不完全满足时，问用户如何处理
* 除开明确定义的setxx、fillxxx等会对参数内属性做调整的方法外，默认所有读写方法**禁止**修改参数内属性值（即只能读取使用）
* 下列所有强制要求必须遵守，所有推荐要求默认也必须遵守，仅当下列情况推荐要求可能不实现时都必须向用户确认：
  * 和强制要求冲突时
  * 和原有项目风格不符合时
  * 不满足实现条件，或者用户需求中隐含要求与推荐规范冲突

## 协作触发
- 默认进入多Agent线性协作。
- 手动触发：`@调度人`。

## 角色与职责
| 角色 | 核心职责 | 子文档 |
|------|----------|--------|
| 调度人 | 需求拆分、上下文压缩、记忆沉淀、流程调度 | `./.claude/agents/orchestrator.md` |
| 架构师 | 方案设计、复用检查、扩展性审查 | `./.claude/agents/architect.md` |
| 编码师 | 按设计实现代码与单测，遵守风格约束 | `./.claude/agents/coder.md` |
| QA | 静态审查、需求验证、测试执行 | `./.claude/agents/qa.md` |

## 核心流程
1. **调度人**拆分需求为可验证断言，写入 `/docs/memory.md` 需求列表。
2. 取首个未完成需求，压缩上下文(>6K抽取方法摘要，>200行架构文档目录抽象)，派发架构师。
3. **架构师**设计方案与复用举证，**若有疑问必须与用户确认**，不得擅自决定。
4. 调度人审核设计，派发编码师实现。
5. **编码师**编码与自测，**遇风格或逻辑疑点必须暂停询问用户**。
6. **QA**审查验证，反馈问题。
7. 调度人标记完成，循环至下一需求。

## 疑问处理原则
- **任何角色对需求、设计、实现存在不确定时，必须暂停并通过调度人向用户确认，严禁自行猜测并修改代码。**

## 记忆库
- 路径：`/docs/memory.md`
- 包含技术栈、需求列表、用户习惯、模块规范、可复用组件等。
- 调度人负责增量更新与去重。

## 规范沉淀
- **项目配置与规范文档统一存入 `.claude/` 目录**
- 示例：Windows命令执行规范 → `.claude/windows-command-spec.md`

## 子文档索引
- 调度人细则：`./.claude/agents/orchestrator.md`
- 架构师细则：`./.claude/agents/architect.md`
- 编码师细则：`./.claude/agents/coder.md`
- QA细则：`./.claude/agents/qa.md`
- 记忆库模板：`./.claude/memory_template.md`

## 项目结构

* 模板地址 `.claude\templates` 的下级目录下的xxx.md为模板
  * storyDemo.md：需求分析和讨论输出的需求文档模板
  * designDemo.md：功能实现的设计模板
  * acceptDemo.md：编码后的质量检查和功能验收模板

## Java基础编码规范

适用场景：所有Java工程

### 命名风格

* 【强制】代码中的命名均不能以下划线或美元符号开始，也不能以下划线或美元符号结束。
* 【强制】严禁使用拼音与英文混合的方式，更不允许直接使用中文。
* 【强制】类名使用UpperCamelCase风格（DO/BO/DTO/VO/AO/PO/UID等除外）。
* 【强制】方法名、参数名、成员变量、局部变量使用lowerCamelCase风格。
* 【强制】常量命名全部大写，单词间用下划线隔开。
* 【强制】抽象类命名使用Abstract或Base开头；异常类命名使用Exception结尾；测试类以被测试类名开头，以Test结尾。
* 【强制】类型与中括号紧挨相连表示数组，如`int[] arrayDemo`。
* 【强制】POJO类中布尔类型的变量都不要加is前缀。
* 【推荐】接口类中的方法和属性不要加任何修饰符（public也不要加），并加上有效的Javadoc注释。
* 【推荐】接口实现类用Impl后缀与接口区别。
* 【参考】枚举类名带上Enum后缀，枚举成员名称全大写，单词间用下划线。

### 常量定义

* 【强制】不允许任何魔法值（未经预先定义的常量）直接出现在代码中。
* 【强制】long/Long赋值时数值后使用大写的L。
* 【推荐】按常量功能进行归类，分开维护，不要使用一个大而全的常量类。
* 【推荐】如果变量值仅在一个固定范围内变化用enum类型定义。

### 代码格式

* 【强制】大括号使用：左大括号前不换行，左大括号后换行，右大括号前换行；右大括号后还有else等代码则不换行，表示终止的右大括号后必须换行。
* 【强制】左小括号和字符之间不出现空格；右小括号和字符之间也不出现空格；左大括号前需要空格。
* 【强制】if/for/while/switch/do等保留字与括号之间都必须加空格。
* 【强制】任何二目、三目运算符的左右两边都需要加一个空格。
* 【强制】采用4个空格缩进，禁止使用tab字符。
* 【强制】单行字符数限制不超过120个，超出需要换行，遵循相应原则。
* 【强制】IDE的text file encoding设置为UTF-8；文件的换行符使用Unix格式。
* 【推荐】单个方法的总行数不超过80行。超过则触发重构机制，提取方法中高耦合的逻辑为子方法，由主方法调用。

### OOP规约

* 【强制】所有覆写方法，必须加@Override注解，方法的JavaDoc注释也必须包含父类的内容，可以扩展注释详细说明。
* 【强制】外部正在调用或者二方库依赖的接口，不允许修改方法签名。
* 【强制】Object的equals方法应使用常量或确定有值的对象来调用，如`"test".equals(object)`。
* 【强制】所有相同类型的包装类对象之间值的比较，全部使用equals方法比较。
* 【强制】POJO类的属性必须使用包装数据类型；RPC方法的返回值和参数必须使用包装数据类型；局部变量推荐使用基本数据类型。
* 【强制】定义DO/DTO/VO等POJO类时，不要设定任何属性默认值。
* 【强制】序列化类新增属性时不要修改serialVersionUID。
* 【强制】构造方法里面禁止加入任何业务逻辑，初始化逻辑放在init方法中。
* 【强制】POJO类必须写toString方法，若使用了Lombok这类插件自行生成可以。
* 【推荐】循环体内字符串连接不能直接使用String的+号链接，推荐使用StringBuilder的append方法或者字符串工具支持传入lambda函数循环拼接。
* 【推荐】类成员与方法访问控制从严（private/protected等）。

### 集合处理

* 【强制】只要重写equals就必须重写hashCode。
* 【强制】ArrayList的subList结果不可强转成ArrayList。
* 【强制】使用集合转数组的方法，必须使用`toArray(T[] array)`，传入类型一致且大小等于list.size()的数组。
* 【强制】使用`Arrays.asList()`把数组转换成集合时，不能使用其修改集合相关的方法（add/remove/clear）。
* 【强制】不要在foreach循环里进行元素的remove/add操作，使用Iterator方式。
* 【推荐】集合初始化时指定初始值大小。
* 【推荐】使用entrySet遍历Map类集合KV，而不是keySet。

### 并发处理

* 【强制】获取单例对象需要保证线程安全。
* 【强制】创建线程或线程池时请指定有意义的线程名称。
* 【强制】线程池不允许使用Executors创建，而应通过ThreadPoolExecutor方式。
* 【强制】SimpleDateFormat是线程不安全的，不要定义为static变量，或者使用日期工具类。
* 【强制】高并发时同步调用需考量锁的性能损耗；能锁区块就不要锁整个方法体。
* 【强制】多线程并行处理定时任务时，使用ScheduledExecutorService而非Timer。
* 【推荐】推荐使用工具类避免Random实例被多线程使用；JDK7后使用ThreadLocalRandom。
* 【参考】volatile解决内存不可见问题，但不能解决线程安全问题（如count++需用Atomic类）。

### 控制语句

* 【强制】switch块内每个case要么以break/return终止，要么注释说明；必须包含default语句放在最后。
* 【强制】if/else/for/while/do语句中必须使用大括号，即使只有一行。
* 【强制】在高并发场景中，避免使用"等于"判断作为中断或退出的条件，改用大于/小于区间条件。
* 【推荐】表达异常分支时少用if-else，可改写为卫语句；if-else嵌套请勿超过3层。
* 【推荐】循环体中的语句要考虑性能，将定义对象、变量、获取连接等操作移至循环体外。

### 注释规约

* 【强制】类、类属性、类方法的注释必须使用Javadoc规范（`/** 内容 */`），不得使用//xxx。
* 【强制】所有的抽象方法（包括接口中的方法）必须用Javadoc注释。
* 【强制】所有的类都必须添加创建者和创建日期。
* 【推荐】代码修改的同时注释也要相应修改。
* 【推荐】注释的内容推荐使用纯文本，不包含HTML标签（Java自身的link等除外，如br、p、div等标签禁止出现），复杂功能说明支持Markdown语法。

## 异常日志

适用场景：异常处理、日志记录

### 异常处理

* 【强制】不要通过catch方式处理可预检查的RuntimeException（如NullPointerException、IndexOutOfBoundsException）。
* 【强制】异常不要用来做流程控制、条件控制。
* 【强制】catch时区分稳定代码和非稳定代码，非稳定代码尽可能区分异常类型处理。
* 【强制】捕获异常后必须处理，如果不想处理则抛给调用者。
* 【强制】finally块必须关闭资源对象、流对象（JDK7+可使用try-with-resources）。
* 【强制】不要在finally块中使用return。
* 【推荐】防止NPE是调用者的责任；注意返回类型为基本数据类型时的自动拆箱NPE等场景。
* 【推荐】定义时区分unchecked/checked异常，避免直接抛出RuntimeException，应使用有业务含义的自定义异常。

### 日志规约

* 【强制】应用中直接使用SLF4J作为日志门面，不可直接使用日志系统（Log4j、Logback）中的API。
* 【强制】对trace/debug/info级别的日志输出必须使用条件输出形式或占位符方式。
* 【推荐】生产环境禁止输出debug日志；谨慎记录日志，避免撑爆磁盘。

## 单元测试

适用场景：编写单元测试

* 【强制】单元测试必须遵守AIR原则（自动化、独立性、可重复）。对于依赖外部文件、网络、数据库的，可以使用mock工具模拟。
* 【强制】单元测试必须全自动执行，使用assert验证，不准使用System.out。
* 【强制】单元测试之间不能互相调用，不能依赖执行次序。
* 【强制】单元测试可重复执行，不受外界环境影响。
* 【强制】测试粒度至多是类级别，一般是方法级别。
* 【推荐】单元测试的基本目标：语句覆盖率达到80%；核心模块语句覆盖率和分支覆盖率均达到100%。
* 【推荐】编写单元测试遵守BCDE原则（边界、正确输入、设计、错误输入）。

## 安全规约

适用场景：通用Java应用

* 【强制】用户敏感数据禁止直接展示，必须脱敏。
* 【强制】用户请求传入的任何参数必须做有效性验证。
* 【强制】使用平台资源（如短信、邮件、下单、支付）时必须实现防重放机制（数量限制、疲劳度控制、验证码校验）。

## 设计规约

### 所有需求与设计遵守

* 【强制】当用户描述的需求中存在**不明确的触发条件、边界值、角色权限、异常情况**时，必须：
  1. 用列表形式明确写出当前理解的关键假设。
  2. 针对每个假设，提出1个具体问题让用户确认。
  3. 在获得确认前，按**最保守**的方式设计（如默认有异常处理）。
* 【强制】对于超过3个状态的业务对象（如任务、流程），必须绘制**状态图**（文字描述版：状态列表 + 合法转换 + 触发条件）。
* 【强制】对于调用链路上涉及超过3个对象的功能，必须绘制**时序图**（文字描述：参与者、步骤、输入输出）。
* 【强制】需求分析必须包含**异常流程**与**业务边界**的显式章节，至少列出：
  * 输入为空/越界时的处理
  * 并发冲突处理（乐观锁/悲观锁）
  * 外部依赖失败（超时/异常）的降级方案
  * 数据一致性要求（强一致/最终一致）
* 【推荐】当涉及超过4个实体且关系复杂时，绘制**类图**（实体+核心字段+关系）。

## AI编码注意

### DRY原则与代码复用

* **【强制】两次以上出现的重复逻辑必须抽取。** 如果发现三段或更多相同/相似的代码块，必须抽成公共方法。
* **【推荐】两段相同逻辑也必须抽取**，以避免未来出现第三段时遗忘。然而，"相同逻辑"仅指**业务含义一致**的逻辑。若两段代码表面相似但业务含义不同（如一个校验用户名格式、一个校验手机号格式），即使正则表达式相同，也不应合并。合并前需判断：未来一处需要变更时，是否会影响另一处的正确行为？若会，则不能合并。
* **【推荐】小心"看似相同"的代码。** 提取前必须判断它们是否会在未来独立演变。如果两段代码只是偶然相似但未来会变化，强行合并反而违反"高内聚低耦合"原则。
* **【推荐】方法总行数不宜超过60行**，超过时提取高耦合逻辑为子方法，80行作为触发阈值。单行字符数限制120个。

### 函数式编程规范

#### Stream流操作

* **【强制】禁止在 `map()`/`filter()`/`peek()` 中修改外部状态。** 这些函数必须是**纯函数**（无副作用）：只做计算/映射/过滤，不修改外部变量，不读写外部状态。**例外**：Java Stream中唯一允许产生副作用的操作是终端操作 `forEach()`，但也应谨慎使用。
* **【强制】每个Stream只能消费一次。** 重复使用会导致 `IllegalStateException`。若需多次遍历，应转为集合或重新创建流。
* **【强制】所有无条件遍历计数场景使用 `list.size()`，禁止使用 `list.stream().count()`。**
* **【强制】必须使用 `toMap(keyMapper, valueMapper, mergeFunction)` 处理键冲突**，其中 `mergeFunction` 必须显式给出合并策略（如 `(k1, k2) -> k1` 保留第一个），严禁出现未指定mergeFunction的情况。
* **【推荐】链式调用行数建议不超过8行**，超出时拆分为私有方法或变量保存中间结果。
* **【推荐】多用方法引用。** 能用 `::` 就不用冗长lambda。
* **【推荐】优化管道顺序。** 优先执行 `filter` 等能尽早减少数据量的操作，再做 `map`/`flatMap`，最后才排序聚合。
* **【推荐】涉及大量整型运算优先使用 `IntStream`/`LongStream`/`DoubleStream`**，避免装箱开销。

#### Optional使用

* **【强制】禁止在类字段或方法参数中使用 `Optional<T>`**，会增加不必要的复杂性并破坏序列化。Optional仅作方法的返回类型使用，明确告知调用者结果可能为空。
* **【强制】严禁直接调用 `Optional.get()` 而不检查。**
* **【推荐】优先用 `orElse`/`orElseGet`/`orElseThrow` 安全获取值，避免 `isPresent()` + `get()` 的组合。**
* **【推荐】创建时机：** 使用 `Optional.of(x)` 当 `x` 确定非空；使用 `Optional.ofNullable(x)` 当 `x` 可能为空；空容器用 `Optional.empty()`。

### 高内聚低耦合与并发

* **【强制】避免深度嵌套的if-else。** 表达异常分支时少用if-else，可改写为卫语句；if-else嵌套请勿超过3层。
* **【强制】ThreadLocal 使用后必须在 finally 块中调用 `remove()` 方法**，防止内存泄漏。
* **【强制】高并发场景避免使用"等于"作为中断条件**，改用大于/小于型判断。

### 工具链与错误输出

* **【强制】禁止使用异常的 `printStackTrace()`** 和 `System.out`/`System.err` 输出日志。
* **【强制】禁止在循环内直接调用文件IO或网络请求**，应改为批量处理后组装数据。
* **【强制】类、类属性、类方法的注释必须使用Javadoc规范（`/** 内容 */`）**，不得使用 `//`。所有抽象方法/接口方法都必须有Javadoc。
* **【强制】所有枚举类需重写 `toString` 方法**，返回有含义的业务描述，提高可读性和序列化友好性。

## 协作流程（需求→设计→计划）

适用场景：用户提出新功能/修改需求

### 总则

* 【强制】收到需求后，判断是否提供需求文档地址（如 `/docs/story/xxx.md`）
  * 有：直接基于该文档分析
  * 无：AI将用户描述整理成Markdown，保存到 `.claude/docs/story/{功能名称}.md`
* 【强制】所有疑问/边界问题追加到需求文档`## 待澄清问题`，用户确认后原地更新（标记✅）
* 【强制】需求分析完成（关键问题已确认）后，编写设计文档到 `.claude/docs/design/{功能名称}.md`，引用需求及本文件规范
* 【强制】设计完成后，编写计划文档到 `.claude/docs/plan/{功能名称}.md`，任务列表格式 `- [ ]`，完成后改 `[x]`
* 【强制】任何问答/确认/变更必须同步更新对应文档（含变更记录）
* 【推荐】文档头部含元数据：创建时间、最后更新、状态、相关文档链接

### 需求文档规范（`.claude/docs/story/xxx.md`）

* 【强制】至少包含：背景与目标、用户角色、功能列表（MoSCoW）、业务规则&边界、异常流程、待澄清问题
* 【强制】AI初次生成时至少列出3~5个边界问题或模糊点
* 【推荐】超3个状态时附文字版状态图（状态+合法转换+触发条件）

### 设计文档规范（`.claude/docs/design/xxx.md`）

* 【强制】包含：后端设计（类、方法、异常处理）、测试要点
* 【强制】无"待讨论"模糊表述；不确定点作为设计问题单独列出并请求决策
* 【推荐】引用规范时用简写，如 `[命名风格·强制]`

### 计划文档规范（`.claude/docs/plan/xxx.md`）

* 【强制】任务列表：`- [ ] 描述 [预估工作量] [依赖]`，按顺序排列
* 【强制】执行任务时标记 `[-]`（进行中），完成后 `[x]`，同步更新文档
* 【推荐】大阶段增加里程碑

### 实时更新与状态同步

* 【强制】问答导致变更必须立即更新对应文档（修改内容+更新时间+变更记录）
* 【强制】每次回复末尾附文档状态摘要，如：
  > 📄 需求文档：`.claude/docs/story/xx.md` 待澄清问题已确认
  > 📐 设计文档：`.claude/docs/design/xx.md` 待审阅
  > ✅ 计划：任务1已完成

* 【推荐】需用户决策时标记 `【需确认】`，集中回答后批量更新

### 目录结构

    项目根/
      .claude/
        docs/
          story/
          design/
          plan/
      src/...
