# Java 整型运算防溢出策略

## 1. 核心原则
在计算时必须先转换以防止溢出问题：
- **`int` 运算**：强制升级为 `long` 计算，天然避免溢出。
- **`long` 运算**：使用 `Math_exact` 抛异常 或 `BigInteger` 保安全。
- **绝对值陷阱**：严禁直接对 `int` 使用 `Math.abs()`，必须先转 `long`后再只用转换的值计算。

## 2. 关键常量
```java
INT_MAX = 2147483647
INT_MIN = -2147483648